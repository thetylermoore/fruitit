<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">

    fruit name:</br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    tell me why you like this fruit:<br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit("Fruit It!")%>
  </div>
<% end %>
